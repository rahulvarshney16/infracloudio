scrape_configs:
          - job_name:       'prom'

                # Override the global default and scrape targets from this job every 5 seconds.
            scrape_interval: 5s
                #
            static_configs:
              - targets: ['172.27.235.69:9393']
                labels:
                  group: 'prometheus'
